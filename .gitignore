node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Embedded Cephalyzer build (synced via npm run sync-ceph) — keep the bundle, skip the sourcemap
static/cephalyzer/**/*.map
